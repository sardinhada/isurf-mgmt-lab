# Log file configuration — edit this before building to redirect log output.
# Must be an absolute path. The directory will be created at runtime if it doesn't exist.
dir=/home/wind/.local/share/isurf-mgmt-alpha/logs
